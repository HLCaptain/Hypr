<UserControl
    x:Class="HyprWinUI3.Views.CustomControls.NoteView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:HyprWinUI3.Views.CustomControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="200"
    d:DesignWidth="200"
    mc:Ignorable="d">

    <Grid>
        <local:EditorElementBackground Element="{x:Bind This}" Model="{x:Bind Note}" />
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48" MinHeight="48" />
                <RowDefinition MinHeight="64" />
            </Grid.RowDefinitions>
            <local:EditorTextboxControl
                x:Name="tbName"
                Grid.Row="0"
                MaxWidth="240"
                Margin="10,5,10,5"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch" />
            <local:EditorTextboxControl
                x:Name="tbText"
                Grid.Row="1"
                MaxWidth="240"
                Margin="10,10,10,10"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch" />
        </Grid>
    </Grid>
</UserControl>
