<UserControl
    x:Class="HyprWinUI3.Views.CustomControls.NoteView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:HyprWinUI3.Views.CustomControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="200"
    d:DesignWidth="200"
    mc:Ignorable="d">

    <Grid PointerMoved="Grid_PointerMoved">
        <Rectangle
            Fill="{ThemeResource SystemControlBaseLowAcrylicWindowBrush}"
            PointerPressed="EditorTextboxControl_PointerPressed"
            PointerReleased="EditorTextboxControl_PointerReleased"
            RadiusX="8"
            RadiusY="8" PointerExited="EditorTextboxControl_PointerExited" />
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48" />
                <RowDefinition Height="Auto" />

            </Grid.RowDefinitions>
            <local:EditorTextboxControl
                x:Name="tbName"
                Grid.Row="0"
                Margin="20,0,20,0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch" />
            <local:EditorTextboxControl
                x:Name="tbText"
                Grid.Row="1"
                Margin="20,10,20,10"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch" />
        </Grid>
    </Grid>
</UserControl>
